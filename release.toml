allow-branch = ["main"]
pre-release-hook = ["just", "_changelog-release", "{{version}}"]
pre-release-commit-message = "chore(release): prepare v{{version}}"
tag-name = "v{{version}}"
tag-message = "chore(release): v{{version}}"
push = true
push-remote = "origin"
publish = true
verify = true
